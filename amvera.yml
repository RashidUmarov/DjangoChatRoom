meta:
  environment: python
  toolchain:
    name: pip
    version: 3.9
build:
  requirementsPath: requirements.txt
run:
  command: gunicorn --bind 0.0.0.0:80 djangochat.djangochat.wsgi:application
  persistenceMount: /data
  containerPort: 80
